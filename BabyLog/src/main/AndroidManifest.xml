<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    package="ru.home.babylog" >

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:name="ru.home.babylog.MyApplication">

        <activity
            android:name="ru.home.babylog.MainActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="ru.home.babylog.AddActivity"
            android:label="@string/app_name_edit"
            android:screenOrientation="portrait"/> <!-- This is for TextInputLayout -->

        <activity
            android:name="ru.home.babylog.BackupActivity"
            android:label="@string/app_name_backup"
            android:screenOrientation="portrait"/>

    </application>


</manifest>
